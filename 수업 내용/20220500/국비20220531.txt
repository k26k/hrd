math - 숫자 관련
Date - 날짜/시간 관련

setInterval(함수, 시간);
-  함수가 시간을 주기로 반복 실행됨(반복)

setTimeout(함수, 시간);
- 시간 만큼 지난 후 함수가 실행됨(일회성)

객체의 생성자 함수 -> class(클래스)
객체의 첫글자는 대문자로 만든다
함수 이름 첫글자가 대문자면 보통 객체

eval(문자) - 문자를 자바스크립트 코드로 바꿔줌


document.queryselector()