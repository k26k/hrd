클래스와 객체

라이브러리형 클래스(외부)
- main이 없는 경우

class 클래스 이름{
  필드 - 정수, 문자형
  생성자
  메서드{}
}

배열 복사
array.arraycopy(복사할 대상 배열, 대상 배열 인덱스, 복사해서 넣을 배열, 넣을 배열 인덱스, 배열의 길이);

cmd로 자바 파일 실행하는법
파일이 있는 위치에서
java java파일이름.java 입력

자바 실행파일은 .jar
file - export기능을 이용 jar file로 배포하기

jar파일 실행
java -jar jar파일.jar